'use client'
import apiClient from "@/util/apiClient"

export default function({name}) {

    const deletePortfolio = () => {

        try {

        } catch (err) {
            
        }

    }


    return (
        <button>

        </button>
    )
}